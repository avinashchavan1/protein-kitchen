# Backend API base URL (Render). Leave unset → app runs local-only (localStorage).
VITE_API_URL=https://protein-kitchen-api.onrender.com

# Google OAuth Web client ID (must match backend GOOGLE_CLIENT_ID).
VITE_GOOGLE_CLIENT_ID=xxxxxxxx.apps.googleusercontent.com
